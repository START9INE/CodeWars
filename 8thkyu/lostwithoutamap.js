Description:

Given an array of integers, return a new array with each value doubled.

For example:

[1, 2, 3] --> [2, 4, 6]


//Simple solution
// Called map with argument num
// This simply iterates through array and multiplies each element by 2 and creates new array.

function maps(x){
    return x.map((num) => num * 2);
    }

